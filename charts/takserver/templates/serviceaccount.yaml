{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "takserver.serviceAccountName" . }}
  labels:
    {{- include "takserver.labels" . | nindent 4 }}
{{- end }}
