{{- include "takserver.deployment" (dict "root" . "role" (dict
  "name" "config"
  "command" (list "sh" "takserver-config-cluster.sh")
  "healthPort" 10800
  "postgres" false
  "ignite" true
  "core" true
  "ports" (list)
)) }}
